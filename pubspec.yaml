name: first_app

environment:
  sdk: "^3.8.0"
  flutter: ">=3.8.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
